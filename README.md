# Spring_Boot_Projects
Project from a Spring Boot Udemy course. Learned to develop a Spring Boot application from scratch, perform CRUD operations against an in-memory database using Spring Data JPA, expose out REST APIs using Spring Web, perform CRUD operations against MYSQL Database, and other Spring Boot fundamentals.
